name: Cargo

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: cargo test --verbose --target "x86_64-pc-windows-msvc"